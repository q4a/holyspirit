uniform sampler2D framebuffer;
uniform vec3 color;

void main()
{
	vec4 temp = (texture2D(framebuffer, gl_TexCoord[0].xy) - vec4( 0.5,0.5,0.5,0.0));
    gl_FragColor = texture2D(framebuffer, gl_TexCoord[0].xy) + temp * vec4(color,1.0);
}


